import React, { Component } from 'react';

import FormArmazena from './FormArmazena';

export default class Armazena extends Component {
    render() {
        return (
            <FormArmazena />
        );
    }
}
